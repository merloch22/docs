---
title: "Variáveis Globais"
---

As variáveis globais são variáveis predefinidas contendo algumas informações tanto dos usuários quanto de si mesmo, essas variáveis obtêm as informações diretamente do whatsapp sem a necessidade de utilizar um input e pode ser utilizadas a qualquer momento a partir do fluxo.

* `menuia-tipo-chat`: Armazena o tipo de chat em uso no WhatsApp (individual ou em grupo).

* `menuia-nome-remetente`: Guarda o nome do remetente associado à interação atual.

* `menuia-numero-remetente`: Mantém o número do remetente ou usuário interagindo com o chatbot.

* `menuia-numero-server`: Armazena seu próprio número

* `menuia-authkey`: Contém a chave de autenticação do usuário atual.

* `menuia-appkey`: Armazena a chave da aplicação (Chave do dispositivo).

  Exemplo de como utilizar uma variável global:

![](/images/fluxo/image7.png)

  Criando variável para ser utilizada (Não precisa atribuir nenhum valor)

Após clicar em "create" a variável será autopreenchida com a informação do próprio servidor:

![](/images/fluxo/image8.png)

Exibe: Olá Nome do Usuário Definido no Whatsapp