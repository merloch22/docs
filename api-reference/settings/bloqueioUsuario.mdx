---
title: 'Bloquear e Desbloquear Usuário'
api: 'POST https://chatbot.menuia.com/api/settings'
---

## Descrição

Este endpoint permite bloquear ou desbloquear um usuário no sistema do chatbot. A operação é controlada pelo valor do parâmetro `message` (block/unblock) e confirmada pelo parâmetro `desBlock`.

### Parâmetros

<ParamField body="appkey" type="string" required>
  Chave da aplicação.
</ParamField>

<ParamField body="authkey" type="string" required>
  Chave de autenticação do usuário.
</ParamField>

<ParamField body="to" type="string" required>
  Número de telefone do usuário a ser bloqueado ou desbloqueado (exemplo: `5581989769960`).
</ParamField>

<ParamField body="message" type="string" required>
  Deve ser `"block"` para bloquear ou `"unblock"` para desbloquear o usuário.
</ParamField>

<ParamField body="desBlock" type="string" required>
  Deve ser `"true"` para confirmar a operação.
</ParamField>

<RequestExample>
```bash cURL
curl -X POST https://chatbot.menuia.com/api/settings \
  -H "Content-Type: application/json" \
  -d '{
    "appkey": "SUA_APPKEY_AQUI",
    "authkey": "SUA_AUTHKEY_AQUI",
    "to": "5581989769960",
    "message": "block",
    "desBlock": "true"
  }'
```

```javascript Node.js
const response = await fetch('https://chatbot.menuia.com/api/settings', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    appkey: 'SUA_APPKEY_AQUI',
    authkey: 'SUA_AUTHKEY_AQUI',
    to: '5581989769960',
    message: 'block',
    desBlock: 'true'
  })
});
```

```python Python
import requests

response = requests.post(
    'https://chatbot.menuia.com/api/settings',
    json={
        'appkey': 'SUA_APPKEY_AQUI',
        'authkey': 'SUA_AUTHKEY_AQUI',
        'to': '5581989769960',
        'message': 'block',
        'desBlock': 'true'
    }
)
```

```php PHP
$response = Http::post('https://chatbot.menuia.com/api/settings', [
    'appkey' => 'SUA_APPKEY_AQUI',
    'authkey' => 'SUA_AUTHKEY_AQUI',
    'to' => '5581989769960',
    'message' => 'block',
    'desBlock' => 'true'
]);
```
</RequestExample>

<ResponseExample>
```json 200 - Sucesso
{
  "status": 200,
  "message": "Usuário bloqueado com sucesso."
}
```

```json 401 - Erro de validação
{
  "status": 401,
  "message": "validation.required"
}
```

```json 404 - Dispositivo não encontrado
{
  "status": 404,
  "message": "Nenhum dispositivo encontrado."
}
```
</ResponseExample>
