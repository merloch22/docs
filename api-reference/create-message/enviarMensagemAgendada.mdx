---
title: "Agendar Mensagem"
api: "POST https://chatbot.menuia.com/api/create-message"
---

## Descrição

Este endpoint permite enviar uma mensagem agendada, seja de texto ou multimídia, para um número ou grupo em um horário específico. A mensagem pode incluir um arquivo de mídia opcional, como imagem ou vídeo.

### Parâmetros

<ParamField body="appkey" type="string" required>
  Chave de aplicação.
</ParamField>

<ParamField body="authkey" type="string" required>
  Chave de autenticação do usuário.
</ParamField>

<ParamField body="to" type="string" required>
  Número de telefone no formato internacional ou ID do grupo. Também pode ser um array codificado em JSON.
</ParamField>

<ParamField body="agendamento" type="string" required>
  Data e hora para envio da mensagem (formato: `YYYY-MM-DD HH:MM:SS`).
</ParamField>

<ParamField body="message" type="string" required>
  Texto da mensagem a ser enviada.
</ParamField>

<ParamField body="file" type="string">
  URL do arquivo multimídia (opcional, caso a mensagem seja multimídia).
</ParamField>

<ParamField body="nomearquivo" type="string">
  Nome do arquivo multimídia (opcional).
</ParamField>

<RequestExample>
```bash cURL
curl -X POST https://chatbot.menuia.com/api/create-message \
  -H "Content-Type: application/json" \
  -d '{
    "appkey": "SUA_APPKEY_AQUI",
    "authkey": "SUA_AUTHKEY_AQUI",
    "to": "+5581989769960",
    "agendamento": "2025-09-24 23:00:00",
    "file": "https://example.com/imagem.jpg",
    "nomearquivo": "imagem.jpg",
    "message": "Esta é uma mensagem agendada com imagem!"
  }'
```

```javascript Node.js
const response = await fetch('https://chatbot.menuia.com/api/create-message', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    appkey: 'SUA_APPKEY_AQUI',
    authkey: 'SUA_AUTHKEY_AQUI',
    to: '+5581989769960',
    agendamento: '2025-09-24 23:00:00',
    file: 'https://example.com/imagem.jpg',
    nomearquivo: 'imagem.jpg',
    message: 'Esta é uma mensagem agendada com imagem!'
  })
});
```

```python Python
import requests

response = requests.post(
    'https://chatbot.menuia.com/api/create-message',
    json={
        'appkey': 'SUA_APPKEY_AQUI',
        'authkey': 'SUA_AUTHKEY_AQUI',
        'to': '+5581989769960',
        'agendamento': '2025-09-24 23:00:00',
        'file': 'https://example.com/imagem.jpg',
        'nomearquivo': 'imagem.jpg',
        'message': 'Esta é uma mensagem agendada com imagem!'
    }
)
```

```php PHP
$response = Http::post('https://chatbot.menuia.com/api/create-message', [
    'appkey' => 'SUA_APPKEY_AQUI',
    'authkey' => 'SUA_AUTHKEY_AQUI',
    'to' => '+5581989769960',
    'agendamento' => '2025-09-24 23:00:00',
    'file' => 'https://example.com/imagem.jpg',
    'nomearquivo' => 'imagem.jpg',
    'message' => 'Esta é uma mensagem agendada com imagem!'
]);
```
</RequestExample>

<ResponseExample>
```json 200 - Sucesso
{
  "status": 200,
  "message": "Mensagem agendada com sucesso.",
  "agendamento_id": "12345"
}
```

```json 401 - Erro de validação
{
  "status": 401,
  "message": "validation.required"
}
```

```json 404 - Dispositivo não encontrado
{
  "status": 404,
  "message": "Nenhum dispositivo encontrado."
}
```
</ResponseExample>
