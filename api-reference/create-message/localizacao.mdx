---
title: 'Enviar Localização'
api: 'POST https://chatbot.menuia.com/api/create-message'
---

<Card title="Webhook relacionado: Localização" icon="bolt" href="/webhook-reference/events/localizacao" horizontal>
  Uma localização dispara o evento **Localização** (com coordenadas e link do mapa).
</Card>

## Descrição

Este endpoint permite enviar uma localização fixa (latitude e longitude) para um número ou grupo. A localização pode ser compartilhada para facilitar o envio de coordenadas geográficas.

### Parâmetros

<ParamField body="appkey" type="string" required>
  Chave de aplicação.
</ParamField>

<ParamField body="authkey" type="string" required>
  Chave de autenticação do usuário.
</ParamField>

<ParamField body="to" type="string" required>
  Número de telefone no formato internacional ou ID do grupo.
</ParamField>

<ParamField body="message" type="string">
  Texto opcional da mensagem a ser enviada junto com a localização.
</ParamField>

<ParamField body="longitude" type="string" required>
  A longitude da localização (em formato decimal).
</ParamField>

<ParamField body="latitude" type="string" required>
  A latitude da localização (em formato decimal).
</ParamField>

<RequestExample>
```bash cURL
curl -X POST https://chatbot.menuia.com/api/create-message \
  -H "Content-Type: application/json" \
  -d '{
    "appkey": "SUA_APPKEY_AQUI",
    "authkey": "SUA_AUTHKEY_AQUI",
    "to": "+5511999999999",
    "message": "Aqui está a localização fixa:",
    "longitude": "25.14",
    "latitude": "52.41"
  }'
```

```javascript Node.js
const response = await fetch('https://chatbot.menuia.com/api/create-message', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    appkey: 'SUA_APPKEY_AQUI',
    authkey: 'SUA_AUTHKEY_AQUI',
    to: '+5511999999999',
    message: 'Aqui está a localização fixa:',
    longitude: '25.14',
    latitude: '52.41'
  })
});
```

```python Python
import requests

response = requests.post(
    'https://chatbot.menuia.com/api/create-message',
    json={
        'appkey': 'SUA_APPKEY_AQUI',
        'authkey': 'SUA_AUTHKEY_AQUI',
        'to': '+5511999999999',
        'message': 'Aqui está a localização fixa:',
        'longitude': '25.14',
        'latitude': '52.41'
    }
)
```

```php PHP
$response = Http::post('https://chatbot.menuia.com/api/create-message', [
    'appkey' => 'SUA_APPKEY_AQUI',
    'authkey' => 'SUA_AUTHKEY_AQUI',
    'to' => '+5511999999999',
    'message' => 'Aqui está a localização fixa:',
    'longitude' => '25.14',
    'latitude' => '52.41'
]);
```
</RequestExample>

<ResponseExample>
```json 200 - Sucesso
{
  "status": 200,
  "message": "Localização enviada com sucesso."
}
```

```json 401 - Erro de validação
{
  "status": 401,
  "message": "validation.required"
}
```

```json 404 - Dispositivo não encontrado
{
  "status": 404,
  "message": "Nenhum dispositivo encontrado."
}
```
</ResponseExample>
