---
title: 'Cancelar Mensagem Agendada'
api: 'POST https://chatbot.menuia.com/api/create-message'
---

## Descrição

Este endpoint permite cancelar uma mensagem agendada com base no ID do agendamento.

### Parâmetros

<ParamField body="appkey" type="string" required>
  Chave de aplicação.
</ParamField>

<ParamField body="authkey" type="string" required>
  Chave de autenticação do usuário.
</ParamField>

<ParamField body="cancelarAgendamento" type="string" required>
  Valor obrigatório, deve ser `"true"` para cancelar o agendamento.
</ParamField>

<ParamField body="message" type="string" required>
  ID do agendamento que deve ser cancelado.
</ParamField>

<RequestExample>
```bash cURL
curl -X POST https://chatbot.menuia.com/api/create-message \
  -H "Content-Type: application/json" \
  -d '{
    "appkey": "SUA_APPKEY_AQUI",
    "authkey": "SUA_AUTHKEY_AQUI",
    "cancelarAgendamento": "true",
    "message": "12345"
  }'
```

```javascript Node.js
const response = await fetch('https://chatbot.menuia.com/api/create-message', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    appkey: 'SUA_APPKEY_AQUI',
    authkey: 'SUA_AUTHKEY_AQUI',
    cancelarAgendamento: 'true',
    message: '12345'
  })
});
```

```python Python
import requests

response = requests.post(
    'https://chatbot.menuia.com/api/create-message',
    json={
        'appkey': 'SUA_APPKEY_AQUI',
        'authkey': 'SUA_AUTHKEY_AQUI',
        'cancelarAgendamento': 'true',
        'message': '12345'
    }
)
```

```php PHP
$response = Http::post('https://chatbot.menuia.com/api/create-message', [
    'appkey' => 'SUA_APPKEY_AQUI',
    'authkey' => 'SUA_AUTHKEY_AQUI',
    'cancelarAgendamento' => 'true',
    'message' => '12345'
]);
```
</RequestExample>

<ResponseExample>
```json 200 - Sucesso
{
  "status": 200,
  "message": "Agendamento cancelado com sucesso."
}
```

```json 401 - Erro de validação
{
  "status": 401,
  "message": "validation.required"
}
```

```json 404 - Agendamento não encontrado
{
  "status": 404,
  "message": "Agendamento não encontrado."
}
```
</ResponseExample>
