---
title: 'Enviar Mídia'
api: "POST https://chatbot.menuia.com/api/create-message"
---

<Card title="Webhook relacionado: Mídia" icon="bolt" href="/webhook-reference/events/imagem" horizontal>
  Mídia recebida dispara os eventos **Imagem**, **Vídeo**, **Áudio** ou **Documento** no webhook.
</Card>

## Descrição

Este endpoint permite o envio de um arquivo multimídia (imagem, áudio, vídeo, documento) para um número de telefone ou grupo, junto com uma mensagem opcional e uma descrição.

### Parâmetros

<ParamField body="appkey" type="string" required>
  Chave de aplicação.
</ParamField>

<ParamField body="authkey" type="string" required>
  Chave de autenticação do usuário.
</ParamField>

<ParamField body="to" type="string" required>
  Número de telefone no formato internacional ou ID do grupo do destinatário.
</ParamField>

<ParamField body="message" type="string">
  Nome do arquivo a ser enviado.
</ParamField>

<ParamField body="descricao" type="string">
  Descrição opcional do arquivo (legenda).
</ParamField>

<ParamField body="file" type="string" required>
  URL ou BASE64 do arquivo multimídia a ser enviado.
</ParamField>

<ParamField body="format" type="string">
  Formato do arquivo (mp3, mp4, pdf, png, etc). Utilizado para forçar o envio em um formato específico.
</ParamField>

<RequestExample>
```bash cURL
curl -X POST https://chatbot.menuia.com/api/create-message \
  -H "Content-Type: application/json" \
  -d '{
    "appkey": "SUA_APPKEY_AQUI",
    "authkey": "SUA_AUTHKEY_AQUI",
    "to": "+5511999999999",
    "message": "Nome do arquivo",
    "descricao": "Descrição opcional",
    "file": "https://example.com/file.mp3",
    "format": "mp3"
  }'
```

```javascript Node.js
const response = await fetch('https://chatbot.menuia.com/api/create-message', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    appkey: 'SUA_APPKEY_AQUI',
    authkey: 'SUA_AUTHKEY_AQUI',
    to: '+5511999999999',
    message: 'Nome do arquivo',
    descricao: 'Descrição opcional',
    file: 'https://example.com/file.mp3',
    format: 'mp3'
  })
});
```

```python Python
import requests

response = requests.post(
    'https://chatbot.menuia.com/api/create-message',
    json={
        'appkey': 'SUA_APPKEY_AQUI',
        'authkey': 'SUA_AUTHKEY_AQUI',
        'to': '+5511999999999',
        'message': 'Nome do arquivo',
        'descricao': 'Descrição opcional',
        'file': 'https://example.com/file.mp3',
        'format': 'mp3'
    }
)
```

```php PHP
$response = Http::post('https://chatbot.menuia.com/api/create-message', [
    'appkey' => 'SUA_APPKEY_AQUI',
    'authkey' => 'SUA_AUTHKEY_AQUI',
    'to' => '+5511999999999',
    'message' => 'Nome do arquivo',
    'descricao' => 'Descrição opcional',
    'file' => 'https://example.com/file.mp3',
    'format' => 'mp3'
]);
```
</RequestExample>

<ResponseExample>
```json 200 - Sucesso
{
  "status": 200,
  "message": "Mensagem enviada com sucesso."
}
```

```json 401 - Erro de validação
{
  "status": 401,
  "message": "validation.required"
}
```

```json 404 - Dispositivo não encontrado
{
  "status": 404,
  "message": "Nenhum dispositivo encontrado."
}
```
</ResponseExample>