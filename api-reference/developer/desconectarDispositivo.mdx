---
title: 'Desconectar Dispositivo'
api: 'POST https://chatbot.menuia.com/api/developer'
---

## Descrição

Este endpoint permite desconectar um dispositivo, fornecendo o ID ou nome do dispositivo que será desconectado.

### Parâmetros

<ParamField body="authkey" type="string" required>
  Chave de autenticação do usuário.
</ParamField>

<ParamField body="message" type="string" required>
  ID ou nome do dispositivo a ser desconectado.
</ParamField>

<ParamField body="desconectar" type="string" required>
  Deve ser "true" para indicar que o dispositivo será desconectado.
</ParamField>

<RequestExample>
```bash cURL
curl -X POST https://chatbot.menuia.com/api/developer \
  -H "Content-Type: application/json" \
  -d '{
    "authkey": "SUA_AUTHKEY_AQUI",
    "message": "ID ou Nome do Dispositivo",
    "desconectar": "true"
  }'
```

```javascript Node.js
const response = await fetch('https://chatbot.menuia.com/api/developer', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    authkey: 'SUA_AUTHKEY_AQUI',
    message: 'ID ou Nome do Dispositivo',
    desconectar: 'true'
  })
});
```

```python Python
import requests

response = requests.post(
    'https://chatbot.menuia.com/api/developer',
    json={
        'authkey': 'SUA_AUTHKEY_AQUI',
        'message': 'ID ou Nome do Dispositivo',
        'desconectar': 'true'
    }
)
```

```php PHP
$response = Http::post('https://chatbot.menuia.com/api/developer', [
    'authkey' => 'SUA_AUTHKEY_AQUI',
    'message' => 'ID ou Nome do Dispositivo',
    'desconectar' => 'true'
]);
```
</RequestExample>

<ResponseExample>
```json 200 - Sucesso
{
  "status": 200,
  "message": "Dispositivo desconectado com sucesso."
}
```

```json 401 - Erro de validação
{
  "status": 401,
  "message": "validation.required"
}
```

```json 404 - Dispositivo não encontrado
{
  "status": 404,
  "message": "Nenhum dispositivo encontrado."
}
```
</ResponseExample>
